[package]
name = "space-chat-core"
version = "0.1.0"
edition = "2021"

[dependencies]
automerge = "0.5"
serde = { version = "1", features = ["derive"] }
uuid = { version = "1", features = ["v4"] }

[dev-dependencies]
serde_json = "1"
